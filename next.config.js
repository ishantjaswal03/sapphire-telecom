/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/sapphire-telecom',
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;
